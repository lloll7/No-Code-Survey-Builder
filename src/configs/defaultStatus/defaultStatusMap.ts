// 该文件用于定义默认状态的映射变化
import SingleSelectDefault from './SingleSelect.ts';
import SinglePicSelect from './SinglePicSelect.ts';

export const defaultStatusMap = {
  'single-select': SingleSelectDefault,
  'single-pic-select': SinglePicSelect,
};
